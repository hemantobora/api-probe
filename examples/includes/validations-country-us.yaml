"GraphQL - Country Lookup":
  status: 200
  body:
    present:
      - "data.country"
      - "data.country.name"
    equals:
      data.country.code: "${COUNTRY_CODE}"
      data.country.capital: "Washington D.C."
    absent:
      - "errors"
